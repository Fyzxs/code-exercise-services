using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace GroceryImport.Core.Tests.DataRecords.ProductRecords
{
    [TestClass]
    public class ProductRecordTests
    {
        /*
         * It's an empty abstract class.
         * The req's call for an object.
         * This class exists because the lack of tests is intentional.
         */
    }
}